name: Code

on: [push]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Dependencies
        uses: ./.github/dependencies

      - name: Lint
        run: yarn lint

  typescript:
    name: TypeScript
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Dependencies
        uses: ./.github/dependencies

      - name: TypeScript
        run: tsc
