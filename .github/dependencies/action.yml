name: Dependencies

runs:
  using: "composite"
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        cache: "yarn"
        node-version: 20

    - name: Install dependencies
      run: yarn
